<%= form_with(model: user_balance_history, local: true) do |form| %>
  <% if user_balance_history.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_balance_history.errors.count, "error") %> prohibited this user_balance_history from being saved:</h2>

      <ul>
      <% user_balance_history.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_balance_id %>
    <%= form.number_field :user_balance_id %>
  </div>

  <div class="field">
    <%= form.label :balance_before %>
    <%= form.number_field :balance_before %>
  </div>

  <div class="field">
    <%= form.label :balance_after %>
    <%= form.number_field :balance_after %>
  </div>

  <div class="field">
    <%= form.label :activity %>
    <%= form.text_field :activity %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
